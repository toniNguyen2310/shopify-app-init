{
    "buildCommand": "npm run build",
    "installCommand": "npm install",
    "outputDirectory": "build",
    "functions": {
        "api/**/*.js": {
            "runtime": "nodejs18.x"
        }
    }
}